# Maren 内购(IAP)配置指南

> 本文配合 `Sources/Support/Store.swift` 与 `Sources/Views/PaywallView.swift` 使用。
> 代码已就绪;以下是「让内购真正能买」所需的配置步骤(分本地测试与上线两段)。

## 产品清单(三个,产品 ID 已写死在代码里)

| 产品 ID | 类型 | 建议定价 | 用途 |
|---|---|---|---|
| `cd.cc.vela.premium.yearly` | 自动续期订阅 | $29.99/年 | 最划算,付费墙默认推荐(置顶) |
| `cd.cc.vela.premium.monthly` | 自动续期订阅 | $3.99/月 | 低门槛 |
| `cd.cc.vela.premium.lifetime` | 非消耗型(一次性买断) | $69.99 | 直击「订阅疲劳」 |

> 年订阅与月订阅必须放进**同一个 Subscription Group**(例如叫 `Maren Premium`)。
> 产品 ID 必须与代码里 `Store.ProductID` 完全一致,否则 `Product.products(for:)` 返回空。

## 已实现的功能边界(首批)

- **付费墙**(`PaywallView`):展示三个产品,价格/周期文案全部取自 StoreKit(随地区变化),含「恢复购买」。
- **entitlement 管理**(`Store`):启动时与交易更新时用 `Transaction.currentEntitlements` 校验;缓存于本机 UserDefaults。
- **首批解锁功能**:「趋势」页的**个性化洞察卡**(`InsightEngine`)。未升级时显示锁定卡 +「升级解锁」按钮。
- **「高级提醒」(Pro)**:经期提前天数自定义(1–5)、PMS/黄体期关怀提醒、按周期阶段的智能提醒(`SmartReminderEngine`)、用药多时段 + 按周几排程。Pro 入口在设置页「Pro · 高级提醒」与用药编辑器。
- **设置入口**:「设置」顶部 `Maren Premium` 区(已激活显示勾,未激活显示升级按钮)+「恢复购买」。

> 哪些功能该锁是产品决策。当前只锁了「个性化洞察」(立项书 V2 明确列为付费)。
> 如需锁更多(主题/Face ID/PCOS/Watch 等),在对应视图加 `if Store.shared.premium { ... } else { /* 锁定态 */ }` 即可,`Store.shared.premium` 是全局可读的 `@Published`。

---

## 第一段:本地测试(无需 App Store Connect)

让模拟器/真机调试时能「加载到产品 + 模拟购买」,需要一份 StoreKit Configuration 文件。Xcode 的 GUI 才能生成合法文件(手写易错),步骤:

1. Xcode → File → New → File… → 搜 **StoreKit Configuration File** → 保存为 `Vela.storekit`(放到项目根目录或 `Resources/`)。
2. 打开 `Vela.storekit`,点左下「+」依次添加三个产品:
   - 两个 **Auto-Renewable Subscription**(放进新建的 Subscription Group `Maren Premium`),Product ID 分别填上面的 yearly / monthly,设置价格与周期(年/月)。
   - 一个 **Non-Consumable In-App Purchase**,Product ID 填 lifetime,设置价格。
3. Edit Scheme → Run → Options → **StoreKit Configuration** 选 `Vela.storekit`。
4. 运行 app,进「设置 → 升级 Maren Premium」或「趋势 → 升级解锁」,即可看到产品并模拟购买/退款。

> 没有 `.storekit` 也能编译运行,只是付费墙会显示「暂时无法加载产品」--这是 `Store.products` 为空时的预期降级,不是 bug。

---

## 第二段:上线(App Store Connect,等注册好账号后)

1. App Store Connect → My Apps → (你的 app) → **Monetization → In-App Purchases**。
2. 创建上面三个产品,ID、类型、价格、本地化文案(显示名 + 描述)与 `.storekit` 里一致。
   - 订阅产品要建 Subscription Group;勾选需要填的**税务与银行业务信息**(否则 IAP 无法上线)。
3. 在 App Store Connect 为每个产品配好**本地化展示名与描述**(用户在付费墙看到的 `product.displayName` / `product.description` 取自这里)。
4. 提交 app 时,首次审核 IAP 需与 app 一起提交审核;之后新增 IAP 可单独提审。
5. 真机用 Sandbox 账号测试购买(Sandbox 账号在 App Store Connect → Users and Access → Sandbox → Tester 创建)。

---

## 隐私与合规要点(已落地,改动时勿回退)

- 购买全程由 Apple 处理,**我们不接触支付信息,也不接触任何健康数据**。
- entitlement 仅缓存于本机 UserDefaults(`store.premium` key),不外传。
- 隐私政策 §6(Purchases)已据此撰写;Scope Note 已注明「除 StoreKit 与 Apple 通信外无其他网络代码」。
- 必须保留「恢复购买」入口(Apple 审核硬性要求),已在 `PaywallView` 与 `SettingsView` 各放一处。

## 代码位置速查

- 产品 ID 与购买/恢复/校验逻辑:`Sources/Support/Store.swift`
- 付费墙 UI:`Sources/Views/PaywallView.swift`
- 洞察锁定卡:`Sources/Views/TrendsView.swift` → `insightsCard`
- 设置入口:`Sources/Views/SettingsView.swift` → `Maren Premium` section
