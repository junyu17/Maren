# Vela / Maren 项目评审报告

> 评审日期:2026-07-27
> 评审范围:`/Users/jun/Documents/Vela` 全量代码 + 工程配置 + 隐私政策 + App Store 资产
> 评审方式:通读全部源码 → XcodeGen 重新生成工程 → 模拟器(iPhone 17)真机构建并启动(--seed-demo)→ 校验 SwiftData 持久化与版本号
> 评审约束:**未修改任何代码**。本报告仅为问题记录与建议。

---

## 0. 评审结论(先看这条)

项目整体质量**很高**,可以作为「免费 MVP」上线,但**当前状态不能直接提审 App Store**——存在 6 项上线阻断问题(P0),其中 2 项会被 App Store 直接拒、1 项是隐私承诺违约。修复 P0 后即可提审。

构建与运行验证:
- `xcodegen generate` ✅
- `xcodebuild ... build` ✅(仅 1 条版本号警告,见 P0-1)
- 模拟器安装 + 启动 + 演示数据播种 ✅
- SwiftData 持久化校验:`default.store` 落盘,含 15 条 PeriodDay / 14 条 DailyLog,跨重启数据保留 ✅

---

## 一、上线阻断问题(P0 · 必须修复)

### P0-1 · Widget 版本号与主 App 不一致 → App Store 必拒

**现象**:`WidgetSources/Info.plist` 把版本号写死:

| | CFBundleShortVersionString | CFBundleVersion |
|---|---|---|
| 主 App(来自 `project.yml` `MARKETING_VERSION`) | `0.1.0` | `1` |
| Widget(Info.plist 硬编码) | `1.0` | `1` |

构建警告:
```
warning: The CFBundleShortVersionString of an app extension ('1.0') must match that of its containing parent app ('0.1.0').
```

**影响**:App Store 审核会以「extension version 不匹配主 app」为由拒绝。每次发版都会复发。

**建议修法**(二选一,任选其一):
1. 把 `WidgetSources/Info.plist` 里的 `CFBundleShortVersionString` 改成 `$(MARKETING_VERSION)`、`CFBundleVersion` 改成 `$(CURRENT_PROJECT_VERSION)`;
2. 或在 `project.yml` 的 `VelaWidgetExtension` target 把 `GENERATE_INFOPLIST_FILE: NO` 改为 `YES`,删除手写 `Info.plist`,用 `INFOPLIST_KEY_*` 注入 `CFBundleDisplayName` / `NSExtension`,版本号自动从 build settings 继承(与 Watch target 一致)。

---

### P0-2 · "删除所有数据" 并未删除全部数据 → 隐私承诺违约

**位置**:`Sources/Views/SettingsView.swift` 第 213–216 行 `deleteAllData()`

```swift
private func deleteAllData() {
    for p in periodDays { context.delete(p) }
    for l in allLogs { context.delete(l) }
    try? context.save()
    ...
}
```

**问题**:数据模型有 5 种(`PeriodDay` / `DailyLog` / `CustomSymptom` / `Medication` / `MedicationIntake`),但这里只删了前两种。用户点「删除所有数据」后,以下数据仍残留在设备里:
- 自定义症状标签(`CustomSymptom`)
- 所有用药/补剂定义(`Medication`,含药名)
- 用药打卡历史(`MedicationIntake`)

按钮文案是「**删除所有数据**」,页脚写「永久删除本机上的**全部**经期与每日记录」,而隐私政策 §9 承诺「permanently delete it」「彻底删除」。这是隐私承诺与实际行为的直接冲突——Flo 当年正是政策漂亮、代码不兑现被罚。

**建议**:补删另外三个模型,并清除相关的本地通知(用药提醒)与 Widget 快照:

```swift
// 示意(不要直接套用,仅说明范围)
for m in medications { NotificationManager.shared.cancelMedicationReminder(id: m.notificationId) }
let meds = (try? context.fetch(FetchDescriptor<Medication>())) ?? []
let intakes = (try? context.fetch(FetchDescriptor<MedicationIntake>())) ?? []
let customs = (try? context.fetch(FetchDescriptor<CustomSymptom>())) ?? []
[meds, intakes, customs].forEach { $0.forEach { context.delete($0) } }
WidgetCenter.shared.reloadAllTimelines()
```
同时把页脚文案与确认弹窗统一为「永久删除全部记录(经期、每日记录、自定义症状、用药与打卡)」,避免「全部」二字只兑现一半。

---

### P0-3 · `DEVELOPMENT_TEAM` 为空 → 无法签名 / 无法提审

**位置**:`project.yml` 第 11 行 `DEVELOPMENT_TEAM: ""`

**影响**:无 Team 无法生成开发/分发证书,无法 Archive、无法上传 App Store Connect。`xcodegen generate` 每次重跑都会把它清回空。

**建议**:填入 Billy 的 Apple Developer Team ID(10 位字母数字,在 App Store Connect / developer.apple.com 账户页可查),或在 Xcode 的 Signing & Capabilities 里手动选 Team 后**不要再跑 `xcodegen generate`**(否则覆盖)。两个方案二选一并固定下来。

---

### P0-4 · StoreKit 完全未实现,但文档/政策已写入「内购」

**现状**:
- 全工程 `grep StoreKit / RevenueCat / Transaction` 命中 0 处,**没有任何内购代码**。
- `CLAUDE.md` 与《立项书》明确定价:Freemium,$3.99/月 / $19.99–24.99/年 / 终身买断 $39–49。
- 隐私政策 §6「Purchases」、§2 表格「Purchase/subscription status」均按「有内购」措辞。

**影响**:定价承诺与代码现状不符。若以当前二进制提审并宣传付费功能,审核会因「找不到内购项」质疑;更糟的是用户期待付费功能却无入口。

**建议**(二选一):
1. **MVP 免费版上线**(推荐,最快):把隐私政策 §6 改为「NOT IN THE CURRENT VERSION」,§2 表格删掉 Purchase 行或标注「v0.1 无内购」;App Store Connect 不配内购。日后加 Premium 再改回。
2. **带内购上线**:补 StoreKit 2 代码(产品配置 `.storekit` 文件、`Product.products(for:)`、`Transaction.updates` 监听、付费墙 UI、 entitlement 校验),在 App Store Connect 创建对应 IAP 项后再提审。

---

### P0-5 · 隐私政策存在占位符 + 内购条款与现状不符

**位置**:`隐私政策_Privacy-Policy.md`

未填占位符(上线前必填,否则政策不生效):
- `> Effective date / 生效日期: [FILL BEFORE LAUNCH]`
- `> Contact / 联系: [your-support-email]`
- `§12 [Developer / entity name] - [your-support-email] - [optional address]`
- `§5 ... (Provider to name here, e.g. TelemetryDeck - or remove this section if no analytics.)`

内容与现状不符:
- §6 Purchases 按「有内购」写,但 v0.1 无内购(见 P0-4)。
- §5 Analytics 已正确标「NOT USED」,✅ 一致。
- §3 iCloud / §4 HealthKit 已正确标「NOT IN THE CURRENT VERSION」,✅ 一致。

**建议**:填全部占位符;§6 按所选上线策略改写(免费版上线就标「NOT IN THE CURRENT VERSION」);政策需托管成可访问 URL 填进 App Store Connect。

---

### P0-6 · 未声明 `ITSAppUsesNonExemptEncryption` → 每次提审要填出口合规问卷

**现状**:`project.yml` 与 Info.plist 均未设置 `ITSAppUsesNonExemptEncryption`。已用 `PlistBuddy` 确认主 app Info.plist 不存在该键。

**影响**:不声明该键,每次上传构建后 App Store Connect 会弹「出口合规」问卷,要求人工确认是否豁免。本 app 仅用标准 HTTPS(无自定义加密),应声明豁免。

**建议**:在 `project.yml` 的 `Vela` target `settings.base` 加:
```yaml
ITSAppUsesNonExemptEncryption: false
```
跑 `xcodegen generate` 后重新构建即可。

---

## 二、重要问题(P1 · 强烈建议修复)

### P1-1 · 周期长度范围三处自相矛盾

| 位置 | 数值 |
|---|---|
| `CycleSettings.swift` `ManualCycle.cycleRange` | `15...90`(用户手动设置上限 90 天) |
| `CyclePredictor.swift` `maxCycle` | `120`(代码实际接受到 120 天) |
| `CyclePredictor.swift` 第 7 行文档注释 | 「支持不规律 / 长周期(15–**90** 天)」 |
| `PCOSInfoView.swift` 面向用户的文案 | 「支持 15–**120** 天的长周期」 |

**矛盾点**:
1. 用户看到 PCOS 文案承诺「支持 15–120 天」,但手动设置周期长度时 Stepper 上限只到 90 天 → 功能与文案不符。
2. 预测器代码接受 120 天,自己的注释却写 90 天 → 代码与注释不一致。
3. 实际记录一个 100 天的长周期,预测器会用它(`maxCycle=120`),但用户没法手动声明自己是 100 天周期。

**建议**:统一为 120 天(对 PCOS 人群更诚实):
- `ManualCycle.cycleRange = 15...120`;
- `CyclePredictor.swift` 第 7 行注释改成「15–120 天」;
- 复查所有「15–90」字样统一为「15–120」。

---

### P1-2 · App Lock 首次启动可能触发两次生物识别鉴权

**位置**:`Sources/Views/RootView.swift` + `Sources/Views/LockView.swift`

`RootView.onChange(of: scenePhase)` 在 `.active` 时调 `lock.authenticate()`;`LockView.onAppear` 又调一次 `lock.authenticate()`。冷启动且锁开启时,这两次几乎同时发生 → 可能出现两个并发的 `LAContext.evaluatePolicy` 调用,导致系统弹窗闪两次或其中一个被取消。

**建议**:二选一保留一处鉴权入口——推荐只在 `LockView.onAppear` 调 `authenticate()`,把 `RootView` 里 `.active` 分支改为「仅当 `!lock.unlocked` 且距上次鉴权失败时」才重试;或加一个「正在鉴权中」的 `@Published var isAuthenticating` 守卫防止重入。

---

### P1-3 · `AppLockManager.canAuthenticate` 为死代码

**位置**:`Sources/Support/AppLockManager.swift` 第 13–16 行定义了 `canAuthenticate`,全工程无任何调用方(`authenticate()` 内部自己重新 `canEvaluatePolicy`)。

**建议**:删除 `canAuthenticate`,或让 `authenticate()` 复用它以避免重复 `LAContext().canEvaluatePolicy` 构造。低风险但属于代码整洁。

---

### P1-4 · `Medication.reminderHour` 范围未校验

`Medication` 的 `reminderHour` 注释写 `0...23`,但 `init` 默认值 9、`MedicationEditor` 用 `DatePicker(.hourAndMinute)`。Picker 不会越界,但若有其他写入路径(如手表/导入)传入越界值,`UNCalendarNotificationTrigger` 会静默不触发。当前无导入路径,风险低。建议在 `scheduleMedicationReminder` 里 clamp 一下作为防御。

---

## 三、次要 / 建议(P2 · 可上线后处理)

### P2-1 · `DemoSeed` 在 Release 路径里

`Sources/Support/DemoSeed.swift` 的 `runIfRequested` 在 `VelaApp.init` 无条件调用,仅靠「启动参数含 `--seed-demo`」判断。真机用户无法传启动参数,功能上安全;但调试代码进了发布二进制。

**建议**:用 `#if DEBUG` 包裹 `DemoSeed.runIfRequested(container)` 调用,或整个 `enum DemoSeed` 加 `#if DEBUG`。

### P2-2 · 首启模拟器出现 CoreData「Failed to stat path」日志噪音

冷启动日志里有大量 `[com.apple.coredata:error] Failed to stat path '.../Library/Application Support/default.store'` 与「Sandbox access to file-write-create denied」。

**实际验证**:随后 `default.store` 成功落盘(110KB,含 15 条 PeriodDay / 14 条 DailyLog),数据跨重启保留 → **持久化实际成功**,日志只是首启建目录过程中的瞬态报错。

**判断**:模拟器首启常见噪音,真机上 SwiftData 会自行创建 `Application Support`。低风险,但建议在真机上回归一次确认无误报。

### P2-3 · AppIcon 色彩空间为 `RGB`(非 sRGB / Display P3)

`sips` 显示 `AppIcon.png` 的 `space: RGB`。Apple 推荐 sRGB 或 Display P3。通常 Asset Catalog 编译器会做转换,审核一般不卡,但建议在图像编辑器里转成 sRGB 一次以彻底规避。

### P2-4 · `project.yml` 配了 HealthKit 用法说明,但未加 capability

`project.yml` 设了 `NSHealthShareUsageDescription` / `NSHealthUpdateUsageDescription`,但 `Vela.entitlements` 无 `com.apple.developer.healthkit`,代码里 `HealthKitBridge.isAvailable` 恒为 false、所有调用安全空转。v0.1 不上线 HealthKit 是合理的(隐私政策 §4 也标注了 NOT IN CURRENT VERSION)。

**建议**:要么暂移除两条 Usage Description(等真要上线 HealthKit 时再加),要么保留但确认审核不会因「声明了用法却无 capability」产生疑问——历史经验是 Apple 不卡,但保持一致更干净。

### P2-5 · Widget `CFBundlePackageType` 为 `XPC!`

手写 `Info.plist` 里 `CFBundlePackageType = XPC!`。这是 app extension 的历史正确值,构建也已成功嵌入。若按 P0-1 改用 `GENERATE_INFOPLIST_FILE: YES`,此键会自动正确生成,无需单独处理。

### P2-6 · 截图尺寸

`AppStore/screenshots/iphone_6.9/*.png` 均为 1320×2868,符合 6.9" iPhone Pro Max 规格要求 ✅。共 7 张,满足 App Store Connect「至少 1 张、建议覆盖全尺寸」的要求。若想覆盖 6.7" 设备展示,可在 Connect 里勾选「使用 6.9" 缩放」即可,无需另出图。

---

## 四、值得肯定的亮点(保留勿改)

1. **隐私优先落地彻底**:`grep` 全工程无任何网络层、无第三方 SDK、无广告/追踪 SDK。隐私叙事与代码一致。
2. **`dayKey` 整数主键**:跨时区 / 夏令时稳定,`Date` 仅作派生只读属性,`#Predicate` 与 `@Query` 排序都正确用 `\.dayKey`。设计正确且贯彻一致。
3. **排卵日「倒推黄体期」而非「第 14 天」**:`PhaseModel.phaseWithin` 用 `cycleLength - lutealLength` 估算排卵,符合立项铁律,且对短周期做了 `min(..., cycleLength - periodDays - 2)` 保护避免排卵落进经期。
4. **免责声明到位**:日历阶段图例下方、预测卡、PhaseInfo、PCOSInfo 均有「统计预测 / 不构成医学建议 / 不可作为避孕或备孕依据」文案。
5. **预测成本有界**:`lookbackDays = 730` + `recentWindow = 6`,单次重算开销不随使用年限增长;日历页只算一次预测再传子视图。
6. **本地化插值合规**:全部 16 个含占位符的 key 在源串用非位置符 `%@` / `%lld`,英文值用 `%1$@` / `%2$lld` 重排语序,符合 CLAUDE.md 铁律。237 个 key 中英文双语 100% 覆盖。
7. **无障碍**:日历格、评分行、症状芯片都补了 `accessibilityLabel` / `accessibilityAddTraits`,颜色编码有等价文字。
8. **诚实叙事**:置信区间随样本量自适应放宽(`minimumHalfWidth`),数据不足时不假装精准;手动模式不谎报「已观测 N 个周期」。
9. **WatchConnectivity 三通道冗余** + **手表不存库**(手机统一 upsert 落库),避免双向同步冲突;`QuickLogApplier` 按 dayKey 幂等。

---

## 五、上线前任务清单(修复 P0 后即可提审)

> 顺序即建议执行顺序。打勾后即可走 App Store Connect 提交流程。

- [ ] **P0-3** 填入 `DEVELOPMENT_TEAM` 到 `project.yml`(或锁定 Xcode 手选 Team,停止再跑 xcodegen)
- [ ] **P0-1** 修 Widget 版本号(改 Info.plist 用 `$(MARKETING_VERSION)`/`$(CURRENT_PROJECT_VERSION)`,或改 `GENERATE_INFOPLIST_FILE: YES`),重新构建确认无版本警告
- [ ] **P0-6** `project.yml` 加 `ITSAppUsesNonExemptEncryption: false`,xcodegen 重新生成
- [ ] **P0-2** `deleteAllData()` 补删 `CustomSymptom` / `Medication` / `MedicationIntake`,取消用药通知,刷新 Widget;同步更新页脚与确认弹窗文案
- [ ] **P0-4** 决定上线策略:① 免费版上线 → 改隐私政策 §6/§2;② 带 IAP → 补 StoreKit 2 代码 + App Store Connect 建 IAP
- [ ] **P0-5** 填隐私政策全部 `[FILL...]` / `[your-...]` 占位符;按 P0-4 决定改写 §6;托管为可访问 URL
- [ ] **P1-1** 统一周期长度范围到 15–120(改 `ManualCycle.cycleRange` + `CyclePredictor` 注释)
- [ ] **P1-2** 修 App Lock 双重鉴权(只保留一处入口或加重入守卫)
- [ ] 真机回归一次(确认 P2-2 的 CoreData 日志在真机不复发,Face ID 锁、通知、Widget、导出 CSV/PDF 走通)
- [ ] App Store Connect:填 App 名称 `Maren: Period & Mood`、副标题、关键词、描述、支持 URL、隐私政策 URL
- [ ] App Store Connect「App Privacy」问卷:如实勾「Data Not Collected / Not Linked to You」(因无任何采集)
- [ ] Archive → Upload → 提审

> P1-3 / P1-4 / P2 全部可在上线后第一个迭代处理,不阻塞提审。

---

*本报告由项目评审生成,未改动任何源代码。所有「建议修法」均为说明,需由开发者落实。*

---

## 六、修复记录(2026-07-27,本次已落实)

> 本次修复**未引入新的已知问题**;构建零警告零错误,模拟器启动正常,StoreKit entitlement 校验链路已跑通。
> 本地化新增 29 个 key(en + zh-Hans),已编译进 bundle 并抽查通过。

### 已修复(代码 / 配置)

| 编号 | 问题 | 修复 |
|---|---|---|
| P0-1 | Widget 版本号与主 app 不一致 | `project.yml` 的 `VelaWidgetExtension.info.properties` 加 `CFBundleShortVersionString: $(MARKETING_VERSION)` / `CFBundleVersion: $(CURRENT_PROJECT_VERSION)`。校验:主 app / Widget / Watch 三者均 `0.1.0 / build 1` ✅ |
| P0-2 | "删除所有数据" 漏删 3 个模型 | `SettingsView.deleteAllData()` 现删全部 5 个模型(`PeriodDay`/`DailyLog`/`CustomSymptom`/`Medication`/`MedicationIntake`),取消用药通知,刷新 Widget 与自定义症状快照;页脚与确认弹窗文案同步更新 |
| P0-4 | 无内购 | 新增 StoreKit 2 实现:`Sources/Support/Store.swift`(产品加载/购买/恢复/`Transaction.updates` 监听/`currentEntitlements` 校验)+ `Sources/Views/PaywallView.swift`(付费墙);「趋势·洞察」卡设为 Premium 首批解锁功能;「设置」加 Maren Premium 区 + 恢复购买。详见 `内购配置指南.md` |
| P0-6 | 未声明出口加密豁免 | `project.yml` 加 `INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO`(裸 build setting 不会注入生成 plist,必须用 `INFOPLIST_KEY_` 前缀)。校验:Info.plist 含 `ITSAppUsesNonExemptEncryption => false` ✅ |
| P1-1 | 周期长度范围三处矛盾 | `ManualCycle.cycleRange` 改 `15...120`;`CyclePredictor` 文档注释改「15–120 天」。现与 `maxCycle=120`、PCOS 文案一致 |
| P1-2 | App Lock 冷启动双弹窗 | `AppLockManager` 加 `isAuthenticating` 重入守卫,`authenticate()` 入口 `guard !unlocked, !isAuthenticating` |
| P1-3 | `canAuthenticate` 死代码 | 删除 |
| P1-4 | `reminderHour` 无越界校验 | `scheduleMedicationReminder` 对 hour/minute 做 `min(max(...))` clamp |
| P2-1 | DemoSeed 进 Release 路径 | `VelaApp.init` 里 `DemoSeed.runIfRequested` 用 `#if DEBUG` 包裹 |
| 配套 | 隐私政策与 StoreKit 现状对齐 | `隐私政策` Scope Note 更新:明确「健康数据仅本机;含 StoreKit 与 Apple 通信,此外无网络代码」 |

### 仍需 Billy 本人完成(代码侧已就绪,这些无法由评审代劳)

| 编号 | 待办 | 原因 |
|---|---|---|
| P0-3 | 在 `project.yml` 填入 `DEVELOPMENT_TEAM`(10 位 Team ID),或锁定 Xcode 手选 Team 后不再跑 `xcodegen generate` | 需 Billy 的 Apple Developer 账号信息 |
| P0-4(收尾) | 注册 App Store Connect 后,创建 3 个 IAP 产品 + (本地测试)在 Xcode 建 `Vela.storekit` + Edit Scheme 选它 | 需 Connect 账号;步骤见 `内购配置指南.md` |
| P0-5 | 填隐私政策的 `[FILL BEFORE LAUNCH]` 生效日期、`[your-support-email]`、`[Developer / entity name]`;并托管为可访问 URL 填进 Connect | 需 Billy 的真实联系信息与上线日期 |
| P0-3 衍生 | Xcode 里给 Vela target 勾选 iCloud(CloudKit)+ HealthKit capability(若要在 v0.1 之后启用同步/Health) | 涉及 Billy 账号;v0.1 可不做 |
| 真机回归 | 真机上跑一遍 Face ID 锁、通知、Widget、导出 CSV/PDF、内购 Sandbox 购买/恢复/退款 | 模拟器已验证无崩溃,真机需 Billy 确认 |

### 本次构建/运行验证

- `xcodegen generate` ✅
- `xcodebuild ... clean build` ✅ **零 error / 零 warning**
- 模拟器安装 + 冷启动 ✅,无 fatal/crash;StoreKit `Transaction.currentEntitlements` 迭代日志正常
- 版本号三者一致 ✅;`ITSAppUsesNonExemptEncryption=false` ✅
- en.lproj 抽查 4 个新 key 翻译命中 ✅

